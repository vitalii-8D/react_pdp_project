// Identical to chat/chat.users-search.tsx - user search doesn't involve real-time transport at
// all, so Chat V2 just reuses the same loader under its own route path.
export { loader } from '../chat/chat.users-search';
